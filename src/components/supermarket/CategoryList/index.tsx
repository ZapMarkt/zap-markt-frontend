import SearchInput from '@/components/ui/SearchInput';

const CategoryTable = () => {
  return (
    <div>
      <SearchInput placeholder="Buscar por categorias" />
    </div>
  );
};

export default CategoryTable;
